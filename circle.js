const piValue = Math.PI
let radius =  parseFloat (prompt(`enter the radius: `));
alert (`the diameter of the circle is:  ${2*radius}`);
alert (`the circumference of the circle is: ${2*piValue*radius}`);
alert (`the area of the circle is: ${piValue*Math.pow(radius,2)}`); 
